interface String {
>         ^^^^^^: any   (Any) (Identifier)
        expected: StringConstructor

interface String {
>         ^^^^^^: any   (Any) (Identifier)
        expected: StringConstructor

    padStart(maxLength: number, fillString?: string): string;
>   ^^^^^^^^: { (maxLength: number, fillString?: string): string; (maxLength: number, fillString?: string): string; }   (Object) (Identifier)
    expected: { (targetLength: number, padString?: string): string; (maxLength: number, fillString?: string): string; }

    padStart(maxLength: number, fillString?: string): string;
>            ^^^^^^^^^: number   (Number) (Identifier)

    padStart(maxLength: number, fillString?: string): string;
>                               ^^^^^^^^^^: string | undefined   (Union) (Identifier)

    padEnd(maxLength: number, fillString?: string): string;
>   ^^^^^^: { (maxLength: number, fillString?: string): string; (maxLength: number, fillString?: string): string; }   (Object) (Identifier)
  expected: { (targetLength: number, padString?: string): string; (maxLength: number, fillString?: string): string; }

    padEnd(maxLength: number, fillString?: string): string;
>          ^^^^^^^^^: number   (Number) (Identifier)

    padEnd(maxLength: number, fillString?: string): string;
>                             ^^^^^^^^^^: string | undefined   (Union) (Identifier)
