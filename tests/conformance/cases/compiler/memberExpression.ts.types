type User = {
>    ^^^^: { name: string; age: number; id: `user_${string}`; active?: boolean; address: { city: string; zip: number; }; greet(message: string): string; preferences: { theme: "light" | "dark"; notifications?: boolean; }; }   (TyObject) (TSTypeAliasDeclaration)

  name: string;
> ^^^^: string   (TyString) (TSPropertySignature)

  age: number;
> ^^^: number   (TyNumber) (TSPropertySignature)

  id: `user_${string}`
> ^^: `user_${string}`   (TyTemplateLiteral) (TSPropertySignature)

  active?: boolean;
> ^^^^^^: boolean | undefined   (TyUnion) (TSPropertySignature)

  address: {
> ^^^^^^^: { city: string; zip: number; }   (TyObject) (TSPropertySignature)

    city: string;
>   ^^^^: string   (TyString) (TSPropertySignature)

    zip: number;
>   ^^^: number   (TyNumber) (TSPropertySignature)

  greet(message: string): string;
> ^^^^^: (message: string) => string   (TyFunction) (TSMethodSignature)

  greet(message: string): string;
>       ^^^^^^^: string   (TyString) (BindingIdentifier)

  preferences: {
> ^^^^^^^^^^^: { theme: "light" | "dark"; notifications?: boolean; }   (TyObject) (TSPropertySignature)

    theme: "light" | "dark";
>   ^^^^^: "light" | "dark"   (TyUnion) (TSPropertySignature)

    notifications?: boolean;
>   ^^^^^^^^^^^^^: boolean | undefined   (TyUnion) (TSPropertySignature)

const user: User = {
>     ^^^^: User   (TyTypeReference) (BindingIdentifier)

const user: User = {
>           ^^^^: any   (TyAny) (TSTypeReference)

  name: "Alice",
> ^^^^: string   (TyString) (ObjectProperty)

  age: 30,
> ^^^: number   (TyNumber) (ObjectProperty)

  id: "user_Alice",
> ^^: "user_Alice"   (TyStringLiteral) (ObjectProperty)

  address: {
> ^^^^^^^: { city: string; zip: number; }   (TyObject) (ObjectProperty)

    city: "Paris",
>   ^^^^: string   (TyString) (ObjectProperty)

    zip: 75000,
>   ^^^: number   (TyNumber) (ObjectProperty)

  greet(message: string) {
> ^^^^^: (message: string) => string   (TyFunction) (ObjectProperty)

  greet(message: string) {
>       ^^^^^^^: string   (TyString) (BindingIdentifier)

    return `${message}, ${this.name}`;
>             ^^^^^^^: string   (TyString) (IdentifierReference)

    return `${message}, ${this.name}`;
>                              ^^^^: string   (TyString) (StaticMemberExpression)

  preferences: {
> ^^^^^^^^^^^: { theme: "dark"; }   (TyObject) (ObjectProperty)

    theme: "dark",
>   ^^^^^: "dark"   (TyStringLiteral) (ObjectProperty)

const userName = user.name;
>     ^^^^^^^^: string   (TyString) (BindingIdentifier)

const userName = user.name;
>                ^^^^: User   (TyTypeReference) (IdentifierReference)

const userName = user.name;
>                     ^^^^: string   (TyString) (StaticMemberExpression)

const userAge = user.age;
>     ^^^^^^^: number   (TyNumber) (BindingIdentifier)

const userAge = user.age;
>               ^^^^: User   (TyTypeReference) (IdentifierReference)

const userAge = user.age;
>                    ^^^: number   (TyNumber) (StaticMemberExpression)

const userId = user.id;
>     ^^^^^^: `user_${string}`   (TyTemplateLiteral) (BindingIdentifier)

const userId = user.id;
>              ^^^^: User   (TyTypeReference) (IdentifierReference)

const userId = user.id;
>                   ^^: `user_${string}`   (TyTemplateLiteral) (StaticMemberExpression)

const userActive = user.active;
>     ^^^^^^^^^^: boolean | undefined   (TyUnion) (BindingIdentifier)

const userActive = user.active;
>                  ^^^^: User   (TyTypeReference) (IdentifierReference)

const userActive = user.active;
>                       ^^^^^^: boolean | undefined   (TyUnion) (StaticMemberExpression)

const userCity = user.address.city;
>     ^^^^^^^^: string   (TyString) (BindingIdentifier)

const userCity = user.address.city;
>                ^^^^: User   (TyTypeReference) (IdentifierReference)

const userCity = user.address.city;
>                     ^^^^^^^: { city: string; zip: number; }   (TyObject) (StaticMemberExpression)

const userCity = user.address.city;
>                             ^^^^: string   (TyString) (StaticMemberExpression)

const userZip = user["address"].zip;
>     ^^^^^^^: number   (TyNumber) (BindingIdentifier)

const userZip = user["address"].zip;
>               ^^^^: User   (TyTypeReference) (IdentifierReference)

const userZip = user["address"].zip;
>                               ^^^: number   (TyNumber) (StaticMemberExpression)

const userGreeting = user.greet("hello");
>     ^^^^^^^^^^^^: string   (TyString) (BindingIdentifier)

const userGreeting = user.greet("hello");
>                    ^^^^: User   (TyTypeReference) (IdentifierReference)

const userGreeting = user.greet("hello");
>                         ^^^^^: (message: string) => string   (TyFunction) (StaticMemberExpression)

const userGreetMethod = user.greet;
>     ^^^^^^^^^^^^^^^: (message: string) => string   (TyFunction) (BindingIdentifier)

const userGreetMethod = user.greet;
>                       ^^^^: User   (TyTypeReference) (IdentifierReference)

const userGreetMethod = user.greet;
>                            ^^^^^: (message: string) => string   (TyFunction) (StaticMemberExpression)

const userTheme = user.preferences.theme;
>     ^^^^^^^^^: "light" | "dark"   (TyUnion) (BindingIdentifier)

const userTheme = user.preferences.theme;
>                 ^^^^: User   (TyTypeReference) (IdentifierReference)

const userTheme = user.preferences.theme;
>                      ^^^^^^^^^^^: { theme: "light" | "dark"; notifications?: boolean; }   (TyObject) (StaticMemberExpression)

const userTheme = user.preferences.theme;
>                                  ^^^^^: "light" | "dark"   (TyUnion) (StaticMemberExpression)

const userNotifications = user.preferences.notifications;
>     ^^^^^^^^^^^^^^^^^: boolean | undefined   (TyUnion) (BindingIdentifier)

const userNotifications = user.preferences.notifications;
>                         ^^^^: User   (TyTypeReference) (IdentifierReference)

const userNotifications = user.preferences.notifications;
>                              ^^^^^^^^^^^: { theme: "light" | "dark"; notifications?: boolean; }   (TyObject) (StaticMemberExpression)

const userNotifications = user.preferences.notifications;
>                                          ^^^^^^^^^^^^^: boolean | undefined   (TyUnion) (StaticMemberExpression)

const literalKey = "name" as const;
>     ^^^^^^^^^^: "name"   (TyStringLiteral) (BindingIdentifier)

const literalKey = "name" as const;
>                            ^^^^^: any   (TyAny) (TSTypeReference)

const unionKey: "name" | "id" = Math.random() > 0.5 ? "name" : "id";
>     ^^^^^^^^: "name" | "id"   (TyUnion) (BindingIdentifier)

const unionKey: "name" | "id" = Math.random() > 0.5 ? "name" : "id";
>                               ^^^^: Math   (TyTypeReference) (IdentifierReference)

const unionKey: "name" | "id" = Math.random() > 0.5 ? "name" : "id";
>                                    ^^^^^^: () => number   (TyFunction) (StaticMemberExpression)

const dynamicStringKey: string = "whatever";
>     ^^^^^^^^^^^^^^^^: string   (TyString) (BindingIdentifier)

const literalKeyValue = user[literalKey];
>     ^^^^^^^^^^^^^^^: string   (TyString) (BindingIdentifier)

const literalKeyValue = user[literalKey];
>                       ^^^^: User   (TyTypeReference) (IdentifierReference)

const literalKeyValue = user[literalKey];
>                            ^^^^^^^^^^: "name"   (TyStringLiteral) (IdentifierReference)

const unionKeyValue = user[unionKey];
>     ^^^^^^^^^^^^^: string   (TyString) (BindingIdentifier)

const unionKeyValue = user[unionKey];
>                     ^^^^: User   (TyTypeReference) (IdentifierReference)

const unionKeyValue = user[unionKey];
>                          ^^^^^^^^: "name" | "id"   (TyUnion) (IdentifierReference)

const dynamicStringKeyValue = user[dynamicStringKey];
>     ^^^^^^^^^^^^^^^^^^^^^: any   (TyAny) (BindingIdentifier)

const dynamicStringKeyValue = user[dynamicStringKey];
>                             ^^^^: User   (TyTypeReference) (IdentifierReference)

const dynamicStringKeyValue = user[dynamicStringKey];
>                                  ^^^^^^^^^^^^^^^^: string   (TyString) (IdentifierReference)

type StringDictionary = {
>    ^^^^^^^^^^^^^^^^: { [key: string]: number; known: number; }   (TyObject) (TSTypeAliasDeclaration)

  known: number;
> ^^^^^: number   (TyNumber) (TSPropertySignature)

  [key: string]: number;
>  ^^^: string   (TyString) (TSIndexSignatureName)

const dictionary: StringDictionary = {
>     ^^^^^^^^^^: StringDictionary   (TyTypeReference) (BindingIdentifier)

const dictionary: StringDictionary = {
>                 ^^^^^^^^^^^^^^^^: any   (TyAny) (TSTypeReference)

  known: 1,
> ^^^^^: number   (TyNumber) (ObjectProperty)

  extra: 2,
> ^^^^^: number   (TyNumber) (ObjectProperty)

const dictionaryKnownDot = dictionary.known;
>     ^^^^^^^^^^^^^^^^^^: number   (TyNumber) (BindingIdentifier)

const dictionaryKnownDot = dictionary.known;
>                          ^^^^^^^^^^: StringDictionary   (TyTypeReference) (IdentifierReference)

const dictionaryKnownDot = dictionary.known;
>                                     ^^^^^: number   (TyNumber) (StaticMemberExpression)

const dictionaryKnownBracket = dictionary["known"];
>     ^^^^^^^^^^^^^^^^^^^^^^: number   (TyNumber) (BindingIdentifier)

const dictionaryKnownBracket = dictionary["known"];
>                              ^^^^^^^^^^: StringDictionary   (TyTypeReference) (IdentifierReference)

const dictionaryExtraDot = dictionary.extra;
>     ^^^^^^^^^^^^^^^^^^: number   (TyNumber) (BindingIdentifier)

const dictionaryExtraDot = dictionary.extra;
>                          ^^^^^^^^^^: StringDictionary   (TyTypeReference) (IdentifierReference)

const dictionaryExtraDot = dictionary.extra;
>                                     ^^^^^: number   (TyNumber) (StaticMemberExpression)

const dictionaryDynamic = dictionary[dynamicStringKey];
>     ^^^^^^^^^^^^^^^^^: number   (TyNumber) (BindingIdentifier)

const dictionaryDynamic = dictionary[dynamicStringKey];
>                         ^^^^^^^^^^: StringDictionary   (TyTypeReference) (IdentifierReference)

const dictionaryDynamic = dictionary[dynamicStringKey];
>                                    ^^^^^^^^^^^^^^^^: string   (TyString) (IdentifierReference)

type NumericDictionary = {
>    ^^^^^^^^^^^^^^^^^: { [index: number]: "zero" | "one" | "many" | undefined; 0: "zero"; 1?: "one"; }   (TyObject) (TSTypeAliasDeclaration)

  [index: number]: "zero" | "one" | "many" | undefined;
>  ^^^^^: number   (TyNumber) (TSIndexSignatureName)

const numericDictionary: NumericDictionary = {
>     ^^^^^^^^^^^^^^^^^: NumericDictionary   (TyTypeReference) (BindingIdentifier)

const numericDictionary: NumericDictionary = {
>                        ^^^^^^^^^^^^^^^^^: any   (TyAny) (TSTypeReference)

const numericZeroDot = numericDictionary[0];
>     ^^^^^^^^^^^^^^: "zero"   (TyStringLiteral) (BindingIdentifier)

const numericZeroDot = numericDictionary[0];
>                      ^^^^^^^^^^^^^^^^^: NumericDictionary   (TyTypeReference) (IdentifierReference)

const numericOneBracket = numericDictionary[1];
>     ^^^^^^^^^^^^^^^^^: "one" | undefined   (TyUnion) (BindingIdentifier)

const numericOneBracket = numericDictionary[1];
>                         ^^^^^^^^^^^^^^^^^: NumericDictionary   (TyTypeReference) (IdentifierReference)

const numericDynamic = numericDictionary[42];
>     ^^^^^^^^^^^^^^: "zero" | "one" | "many" | undefined   (TyUnion) (BindingIdentifier)

const numericDynamic = numericDictionary[42];
>                      ^^^^^^^^^^^^^^^^^: NumericDictionary   (TyTypeReference) (IdentifierReference)

const users = [user, { ...user, name: "Bob", id: "user_Bob" }] as const;
>     ^^^^^: readonly [User, { readonly name: "Bob"; readonly id: "user_Bob"; readonly age: number; readonly active?: boolean; readonly address: { city: string; zip: number; }; readonly greet: (message: string) => string; readonly preferences: { theme: "light" | "dark"; notifications?: boolean; }; }]   (TyTuple) (BindingIdentifier)

const users = [user, { ...user, name: "Bob", id: "user_Bob" }] as const;
>              ^^^^: User   (TyTypeReference) (IdentifierReference)

const users = [user, { ...user, name: "Bob", id: "user_Bob" }] as const;
>                         ^^^^: User   (TyTypeReference) (IdentifierReference)

const users = [user, { ...user, name: "Bob", id: "user_Bob" }] as const;
>                               ^^^^: "Bob"   (TyStringLiteral) (ObjectProperty)

const users = [user, { ...user, name: "Bob", id: "user_Bob" }] as const;
>                                            ^^: "user_Bob"   (TyStringLiteral) (ObjectProperty)

const users = [user, { ...user, name: "Bob", id: "user_Bob" }] as const;
>                                                                 ^^^^^: any   (TyAny) (TSTypeReference)

const firstUser = users[0];
>     ^^^^^^^^^: User   (TyTypeReference) (BindingIdentifier)

const firstUser = users[0];
>                 ^^^^^: readonly [User, { readonly name: "Bob"; readonly id: "user_Bob"; readonly age: number; readonly active?: boolean; readonly address: { city: string; zip: number; }; readonly greet: (message: string) => string; readonly preferences: { theme: "light" | "dark"; notifications?: boolean; }; }]   (TyTuple) (IdentifierReference)

const firstUserName = users[0].name;
>     ^^^^^^^^^^^^^: string   (TyString) (BindingIdentifier)

const firstUserName = users[0].name;
>                     ^^^^^: readonly [User, { readonly name: "Bob"; readonly id: "user_Bob"; readonly age: number; readonly active?: boolean; readonly address: { city: string; zip: number; }; readonly greet: (message: string) => string; readonly preferences: { theme: "light" | "dark"; notifications?: boolean; }; }]   (TyTuple) (IdentifierReference)

const firstUserName = users[0].name;
>                              ^^^^: string   (TyString) (StaticMemberExpression)

const usersLength = users.length;
>     ^^^^^^^^^^^: 2   (TyNumberLiteral) (BindingIdentifier)

const usersLength = users.length;
>                   ^^^^^: readonly [User, { readonly name: "Bob"; readonly id: "user_Bob"; readonly age: number; readonly active?: boolean; readonly address: { city: string; zip: number; }; readonly greet: (message: string) => string; readonly preferences: { theme: "light" | "dark"; notifications?: boolean; }; }]   (TyTuple) (IdentifierReference)

const usersLength = users.length;
>                         ^^^^^^: 2   (TyNumberLiteral) (StaticMemberExpression)

const arrayMapMethod = users.map;
>     ^^^^^^^^^^^^^^: <U>(callbackfn: (value: User | { readonly name: "Bob"; readonly id: "user_Bob"; readonly age: number; readonly active?: boolean; readonly address: { city: string; zip: number; }; readonly greet: (message: string) => string; readonly preferences: { theme: "light" | "dark"; notifications?: boolean; }; }, index: number, array: readonly (User | { readonly name: "Bob"; readonly id: "user_Bob"; readonly age: number; readonly active?: boolean; readonly address: { city: string; zip: number; }; readonly greet: (message: string) => string; readonly preferences: { theme: "light" | "dark"; notifications?: boolean; }; })[]) => U, thisArg?: any) => U[]   (TyFunction) (BindingIdentifier)

const arrayMapMethod = users.map;
>                      ^^^^^: readonly [User, { readonly name: "Bob"; readonly id: "user_Bob"; readonly age: number; readonly active?: boolean; readonly address: { city: string; zip: number; }; readonly greet: (message: string) => string; readonly preferences: { theme: "light" | "dark"; notifications?: boolean; }; }]   (TyTuple) (IdentifierReference)

const arrayMapMethod = users.map;
>                            ^^^: <U>(callbackfn: (value: User | { readonly name: "Bob"; readonly id: "user_Bob"; readonly age: number; readonly active?: boolean; readonly address: { city: string; zip: number; }; readonly greet: (message: string) => string; readonly preferences: { theme: "light" | "dark"; notifications?: boolean; }; }, index: number, array: readonly (User | { readonly name: "Bob"; readonly id: "user_Bob"; readonly age: number; readonly active?: boolean; readonly address: { city: string; zip: number; }; readonly greet: (message: string) => string; readonly preferences: { theme: "light" | "dark"; notifications?: boolean; }; })[]) => U, thisArg?: any) => U[]   (TyFunction) (StaticMemberExpression)

const tuple = ["ready", 200, { ok: true }] as const;
>     ^^^^^: readonly ["ready", 200, { readonly ok: true; }]   (TyTuple) (BindingIdentifier)

const tuple = ["ready", 200, { ok: true }] as const;
>                              ^^: true   (TyBooleanLiteral) (ObjectProperty)

const tuple = ["ready", 200, { ok: true }] as const;
>                                             ^^^^^: any   (TyAny) (TSTypeReference)

const tupleStatus = tuple[0];
>     ^^^^^^^^^^^: "ready"   (TyStringLiteral) (BindingIdentifier)

const tupleStatus = tuple[0];
>                   ^^^^^: readonly ["ready", 200, { readonly ok: true; }]   (TyTuple) (IdentifierReference)

const tupleCode = tuple[1];
>     ^^^^^^^^^: 200   (TyNumberLiteral) (BindingIdentifier)

const tupleCode = tuple[1];
>                 ^^^^^: readonly ["ready", 200, { readonly ok: true; }]   (TyTuple) (IdentifierReference)

const tupleObjectOk = tuple[2].ok;
>     ^^^^^^^^^^^^^: true   (TyBooleanLiteral) (BindingIdentifier)

const tupleObjectOk = tuple[2].ok;
>                     ^^^^^: readonly ["ready", 200, { readonly ok: true; }]   (TyTuple) (IdentifierReference)

const tupleObjectOk = tuple[2].ok;
>                              ^^: true   (TyBooleanLiteral) (StaticMemberExpression)

const tupleLength = tuple.length;
>     ^^^^^^^^^^^: 3   (TyNumberLiteral) (BindingIdentifier)

const tupleLength = tuple.length;
>                   ^^^^^: readonly ["ready", 200, { readonly ok: true; }]   (TyTuple) (IdentifierReference)

const tupleLength = tuple.length;
>                         ^^^^^^: 3   (TyNumberLiteral) (StaticMemberExpression)

const tupleBracketLength = tuple["length"];
>     ^^^^^^^^^^^^^^^^^^: 3   (TyNumberLiteral) (BindingIdentifier)

const tupleBracketLength = tuple["length"];
>                          ^^^^^: readonly ["ready", 200, { readonly ok: true; }]   (TyTuple) (IdentifierReference)

declare const optionalTuple: readonly [string, number?];
>             ^^^^^^^^^^^^^: readonly [string, (number | undefined)?]   (TyTuple) (BindingIdentifier)

const optionalTupleLength = optionalTuple.length;
>     ^^^^^^^^^^^^^^^^^^^: 1 | 2   (TyUnion) (BindingIdentifier)

const optionalTupleLength = optionalTuple.length;
>                           ^^^^^^^^^^^^^: readonly [string, (number | undefined)?]   (TyTuple) (IdentifierReference)

const optionalTupleLength = optionalTuple.length;
>                                         ^^^^^^: 1 | 2   (TyUnion) (StaticMemberExpression)

const optionalTupleBracketLength = optionalTuple["length"];
>     ^^^^^^^^^^^^^^^^^^^^^^^^^^: 1 | 2   (TyUnion) (BindingIdentifier)

const optionalTupleBracketLength = optionalTuple["length"];
>                                  ^^^^^^^^^^^^^: readonly [string, (number | undefined)?]   (TyTuple) (IdentifierReference)

declare const restTuple: readonly [string, ...number[]];
>             ^^^^^^^^^: readonly [string, ...number[]]   (TyTuple) (BindingIdentifier)

const restTupleLength = restTuple.length;
>     ^^^^^^^^^^^^^^^: number   (TyNumber) (BindingIdentifier)

const restTupleLength = restTuple.length;
>                       ^^^^^^^^^: readonly [string, ...number[]]   (TyTuple) (IdentifierReference)

const restTupleLength = restTuple.length;
>                                 ^^^^^^: number   (TyNumber) (StaticMemberExpression)

const restTupleBracketLength = restTuple["length"];
>     ^^^^^^^^^^^^^^^^^^^^^^: number   (TyNumber) (BindingIdentifier)

const restTupleBracketLength = restTuple["length"];
>                              ^^^^^^^^^: readonly [string, ...number[]]   (TyTuple) (IdentifierReference)

const matrix = [[1, 2], [3, 4]] as const;
>     ^^^^^^: readonly [readonly [1, 2], readonly [3, 4]]   (TyTuple) (BindingIdentifier)

const matrix = [[1, 2], [3, 4]] as const;
>                                  ^^^^^: any   (TyAny) (TSTypeReference)

const matrixFirstRow = matrix[0];
>     ^^^^^^^^^^^^^^: readonly [1, 2]   (TyTuple) (BindingIdentifier)

const matrixFirstRow = matrix[0];
>                      ^^^^^^: readonly [readonly [1, 2], readonly [3, 4]]   (TyTuple) (IdentifierReference)

const matrixFirstValue = matrix[0][0];
>     ^^^^^^^^^^^^^^^^: 1   (TyNumberLiteral) (BindingIdentifier)

const matrixFirstValue = matrix[0][0];
>                        ^^^^^^: readonly [readonly [1, 2], readonly [3, 4]]   (TyTuple) (IdentifierReference)

type CallableWithProperty = {
>    ^^^^^^^^^^^^^^^^^^^^: { (value: number): string; description: string; }   (TyObject) (TSTypeAliasDeclaration)

  (value: number): string;
>  ^^^^^: number   (TyNumber) (BindingIdentifier)

  description: string;
> ^^^^^^^^^^^: string   (TyString) (TSPropertySignature)

declare const callableWithProperty: CallableWithProperty;
>             ^^^^^^^^^^^^^^^^^^^^: CallableWithProperty   (TyTypeReference) (BindingIdentifier)

declare const callableWithProperty: CallableWithProperty;
>                                   ^^^^^^^^^^^^^^^^^^^^: any   (TyAny) (TSTypeReference)

const callableResult = callableWithProperty(123);
>     ^^^^^^^^^^^^^^: string   (TyString) (BindingIdentifier)

const callableResult = callableWithProperty(123);
>                      ^^^^^^^^^^^^^^^^^^^^: CallableWithProperty   (TyTypeReference) (IdentifierReference)

const callableDescription = callableWithProperty.description;
>     ^^^^^^^^^^^^^^^^^^^: string   (TyString) (BindingIdentifier)

const callableDescription = callableWithProperty.description;
>                           ^^^^^^^^^^^^^^^^^^^^: CallableWithProperty   (TyTypeReference) (IdentifierReference)

const callableDescription = callableWithProperty.description;
>                                                ^^^^^^^^^^^: string   (TyString) (StaticMemberExpression)

const callableApply = callableWithProperty.apply;
>     ^^^^^^^^^^^^^: { <T, R>(this: (this: T) => R, thisArg: T): R; <T, A extends any[], R>(this: (this: T, ...args: A) => R, thisArg: T, args: A): R; }   (TyObject) (BindingIdentifier)

const callableApply = callableWithProperty.apply;
>                     ^^^^^^^^^^^^^^^^^^^^: CallableWithProperty   (TyTypeReference) (IdentifierReference)

const callableApply = callableWithProperty.apply;
>                                          ^^^^^: { <T, R>(this: (this: T) => R, thisArg: T): R; <T, A extends any[], R>(this: (this: T, ...args: A) => R, thisArg: T, args: A): R; }   (TyObject) (StaticMemberExpression)

class Counter {
>     ^^^^^^^: typeof Counter   (TyTypeQuery) (BindingIdentifier)

  static label = "counter";
>        ^^^^^: string   (TyString) (PropertyDefinition)

  static create() {
>        ^^^^^^: () => Counter   (TyFunction) (MethodDefinition)

    return new Counter(0);
>              ^^^^^^^: typeof Counter   (TyTypeQuery) (IdentifierReference)

  constructor(public value: number) {}
>                    ^^^^^: number   (TyNumber) (BindingIdentifier)

  increment() {
> ^^^^^^^^^: () => number   (TyFunction) (MethodDefinition)

    this.value += 1;
>   ^^^^^^^^^^^^^^^^: number   (TyNumber) (ExpressionStatement)

    this.value += 1;
>        ^^^^^: number   (TyNumber) (StaticMemberExpression)

    return this.value;
>               ^^^^^: number   (TyNumber) (StaticMemberExpression)

  readSecret() {
> ^^^^^^^^^^: () => number   (TyFunction) (MethodDefinition)

const counter = new Counter(1);
>     ^^^^^^^: Counter   (TyTypeReference) (BindingIdentifier)

const counter = new Counter(1);
>                   ^^^^^^^: typeof Counter   (TyTypeQuery) (IdentifierReference)

const counterValue = counter.value;
>     ^^^^^^^^^^^^: number   (TyNumber) (BindingIdentifier)

const counterValue = counter.value;
>                    ^^^^^^^: Counter   (TyTypeReference) (IdentifierReference)

const counterValue = counter.value;
>                            ^^^^^: number   (TyNumber) (StaticMemberExpression)

const counterIncrement = counter.increment;
>     ^^^^^^^^^^^^^^^^: () => number   (TyFunction) (BindingIdentifier)

const counterIncrement = counter.increment;
>                        ^^^^^^^: Counter   (TyTypeReference) (IdentifierReference)

const counterIncrement = counter.increment;
>                                ^^^^^^^^^: () => number   (TyFunction) (StaticMemberExpression)

const counterIncrementResult = counter.increment();
>     ^^^^^^^^^^^^^^^^^^^^^^: number   (TyNumber) (BindingIdentifier)

const counterIncrementResult = counter.increment();
>                              ^^^^^^^: Counter   (TyTypeReference) (IdentifierReference)

const counterIncrementResult = counter.increment();
>                                      ^^^^^^^^^: () => number   (TyFunction) (StaticMemberExpression)

const counterSecret = counter.readSecret();
>     ^^^^^^^^^^^^^: number   (TyNumber) (BindingIdentifier)

const counterSecret = counter.readSecret();
>                     ^^^^^^^: Counter   (TyTypeReference) (IdentifierReference)

const counterSecret = counter.readSecret();
>                             ^^^^^^^^^^: () => number   (TyFunction) (StaticMemberExpression)

const counterStaticLabel = Counter.label;
>     ^^^^^^^^^^^^^^^^^^: string   (TyString) (BindingIdentifier)

const counterStaticLabel = Counter.label;
>                          ^^^^^^^: typeof Counter   (TyTypeQuery) (IdentifierReference)

const counterStaticLabel = Counter.label;
>                                  ^^^^^: string   (TyString) (StaticMemberExpression)

const counterStaticFactory = Counter.create;
>     ^^^^^^^^^^^^^^^^^^^^: () => Counter   (TyFunction) (BindingIdentifier)

const counterStaticFactory = Counter.create;
>                            ^^^^^^^: typeof Counter   (TyTypeQuery) (IdentifierReference)

const counterStaticFactory = Counter.create;
>                                    ^^^^^^: () => Counter   (TyFunction) (StaticMemberExpression)

const counterStaticResult = Counter.create();
>     ^^^^^^^^^^^^^^^^^^^: Counter   (TyTypeReference) (BindingIdentifier)

const counterStaticResult = Counter.create();
>                           ^^^^^^^: typeof Counter   (TyTypeQuery) (IdentifierReference)

const counterStaticResult = Counter.create();
>                                   ^^^^^^: () => Counter   (TyFunction) (StaticMemberExpression)

class DerivedCounter extends Counter {
>     ^^^^^^^^^^^^^^: typeof DerivedCounter   (TyTypeQuery) (BindingIdentifier)

class DerivedCounter extends Counter {
>                            ^^^^^^^: typeof Counter   (TyTypeQuery) (IdentifierReference)

  readSuperIncrement() {
> ^^^^^^^^^^^^^^^^^^: () => number   (TyFunction) (MethodDefinition)

    return super.increment();
>                ^^^^^^^^^: () => number   (TyFunction) (StaticMemberExpression)

const derived = new DerivedCounter(5);
>     ^^^^^^^: DerivedCounter   (TyTypeReference) (BindingIdentifier)

const derived = new DerivedCounter(5);
>                   ^^^^^^^^^^^^^^: typeof DerivedCounter   (TyTypeQuery) (IdentifierReference)

const derivedSuperResult = derived.readSuperIncrement();
>     ^^^^^^^^^^^^^^^^^^: number   (TyNumber) (BindingIdentifier)

const derivedSuperResult = derived.readSuperIncrement();
>                          ^^^^^^^: DerivedCounter   (TyTypeReference) (IdentifierReference)

const derivedSuperResult = derived.readSuperIncrement();
>                                  ^^^^^^^^^^^^^^^^^^: () => number   (TyFunction) (StaticMemberExpression)

const uniqueKey: unique symbol = Symbol("uniqueKey");
>     ^^^^^^^^^: unique symbol   (TyUniqueSymbol) (BindingIdentifier)

const uniqueKey: unique symbol = Symbol("uniqueKey");
>                                ^^^^^^: SymbolConstructor   (TyTypeReference) (IdentifierReference)

type SymbolObject = {
>    ^^^^^^^^^^^^: { regular: string; [uniqueKey]: number; }   (TyObject) (TSTypeAliasDeclaration)

  [uniqueKey]: number;
>  ^^^^^^^^^: number   (TyNumber) (TSPropertySignature)

  [uniqueKey]: number;
>  ^^^^^^^^^: unique symbol   (TyUniqueSymbol) (IdentifierReference)

  regular: string;
> ^^^^^^^: string   (TyString) (TSPropertySignature)

const symbolObject: SymbolObject = {
>     ^^^^^^^^^^^^: SymbolObject   (TyTypeReference) (BindingIdentifier)

const symbolObject: SymbolObject = {
>                   ^^^^^^^^^^^^: any   (TyAny) (TSTypeReference)

  [uniqueKey]: 42,
>  ^^^^^^^^^: number   (TyNumber) (ObjectProperty)

  [uniqueKey]: 42,
>  ^^^^^^^^^: unique symbol   (TyUniqueSymbol) (IdentifierReference)

  regular: "ok",
> ^^^^^^^: string   (TyString) (ObjectProperty)

const symbolValue = symbolObject[uniqueKey];
>     ^^^^^^^^^^^: number   (TyNumber) (BindingIdentifier)

const symbolValue = symbolObject[uniqueKey];
>                   ^^^^^^^^^^^^: SymbolObject   (TyTypeReference) (IdentifierReference)

const symbolValue = symbolObject[uniqueKey];
>                                ^^^^^^^^^: unique symbol   (TyUniqueSymbol) (IdentifierReference)

const symbolRegular = symbolObject.regular;
>     ^^^^^^^^^^^^^: string   (TyString) (BindingIdentifier)

const symbolRegular = symbolObject.regular;
>                     ^^^^^^^^^^^^: SymbolObject   (TyTypeReference) (IdentifierReference)

const symbolRegular = symbolObject.regular;
>                                  ^^^^^^^: string   (TyString) (StaticMemberExpression)

type Hyphenated = {
>    ^^^^^^^^^^: { "data-id": string; "aria-label"?: string; }   (TyObject) (TSTypeAliasDeclaration)

const hyphenated: Hyphenated = {
>     ^^^^^^^^^^: Hyphenated   (TyTypeReference) (BindingIdentifier)

const hyphenated: Hyphenated = {
>                 ^^^^^^^^^^: any   (TyAny) (TSTypeReference)

const hyphenatedDataId = hyphenated["data-id"];
>     ^^^^^^^^^^^^^^^^: string   (TyString) (BindingIdentifier)

const hyphenatedDataId = hyphenated["data-id"];
>                        ^^^^^^^^^^: Hyphenated   (TyTypeReference) (IdentifierReference)

const hyphenatedAriaLabel = hyphenated["aria-label"];
>     ^^^^^^^^^^^^^^^^^^^: string | undefined   (TyUnion) (BindingIdentifier)

const hyphenatedAriaLabel = hyphenated["aria-label"];
>                           ^^^^^^^^^^: Hyphenated   (TyTypeReference) (IdentifierReference)

type UnionMember =
>    ^^^^^^^^^^^: { kind: "text"; value: string; common: boolean; } | { kind: "count"; value: number; common: boolean; }   (TyUnion) (TSTypeAliasDeclaration)

  | { kind: "text"; value: string; common: boolean }
>     ^^^^: "text"   (TyStringLiteral) (TSPropertySignature)

  | { kind: "text"; value: string; common: boolean }
>                   ^^^^^: string   (TyString) (TSPropertySignature)

  | { kind: "text"; value: string; common: boolean }
>                                  ^^^^^^: boolean   (TyBoolean) (TSPropertySignature)

  | { kind: "count"; value: number; common: boolean }
>     ^^^^: "count"   (TyStringLiteral) (TSPropertySignature)

  | { kind: "count"; value: number; common: boolean }
>                    ^^^^^: number   (TyNumber) (TSPropertySignature)

  | { kind: "count"; value: number; common: boolean }
>                                   ^^^^^^: boolean   (TyBoolean) (TSPropertySignature)

declare const unionMember: UnionMember;
>             ^^^^^^^^^^^: UnionMember   (TyTypeReference) (BindingIdentifier)

declare const unionMember: UnionMember;
>                          ^^^^^^^^^^^: any   (TyAny) (TSTypeReference)

const unionKind = unionMember.kind;
>     ^^^^^^^^^: "text" | "count"   (TyUnion) (BindingIdentifier)

const unionKind = unionMember.kind;
>                 ^^^^^^^^^^^: UnionMember   (TyTypeReference) (IdentifierReference)

const unionKind = unionMember.kind;
>                             ^^^^: "text" | "count"   (TyUnion) (StaticMemberExpression)

const unionValue = unionMember.value;
>     ^^^^^^^^^^: string | number   (TyUnion) (BindingIdentifier)

const unionValue = unionMember.value;
>                  ^^^^^^^^^^^: UnionMember   (TyTypeReference) (IdentifierReference)

const unionValue = unionMember.value;
>                              ^^^^^: string | number   (TyUnion) (StaticMemberExpression)

const unionCommon = unionMember.common;
>     ^^^^^^^^^^^: boolean   (TyBoolean) (BindingIdentifier)

const unionCommon = unionMember.common;
>                   ^^^^^^^^^^^: UnionMember   (TyTypeReference) (IdentifierReference)

const unionCommon = unionMember.common;
>                               ^^^^^^: boolean   (TyBoolean) (StaticMemberExpression)

type IntersectionMember = { left: string } & { right: number };
>    ^^^^^^^^^^^^^^^^^^: { left: string; } & { right: number; }   (TyIntersection) (TSTypeAliasDeclaration)

type IntersectionMember = { left: string } & { right: number };
>                           ^^^^: string   (TyString) (TSPropertySignature)

type IntersectionMember = { left: string } & { right: number };
>                                              ^^^^^: number   (TyNumber) (TSPropertySignature)

declare const intersectionMember: IntersectionMember;
>             ^^^^^^^^^^^^^^^^^^: IntersectionMember   (TyTypeReference) (BindingIdentifier)

declare const intersectionMember: IntersectionMember;
>                                 ^^^^^^^^^^^^^^^^^^: any   (TyAny) (TSTypeReference)

const intersectionLeft = intersectionMember.left;
>     ^^^^^^^^^^^^^^^^: string   (TyString) (BindingIdentifier)

const intersectionLeft = intersectionMember.left;
>                        ^^^^^^^^^^^^^^^^^^: IntersectionMember   (TyTypeReference) (IdentifierReference)

const intersectionLeft = intersectionMember.left;
>                                           ^^^^: string   (TyString) (StaticMemberExpression)

const intersectionRight = intersectionMember.right;
>     ^^^^^^^^^^^^^^^^^: number   (TyNumber) (BindingIdentifier)

const intersectionRight = intersectionMember.right;
>                         ^^^^^^^^^^^^^^^^^^: IntersectionMember   (TyTypeReference) (IdentifierReference)

const intersectionRight = intersectionMember.right;
>                                            ^^^^^: number   (TyNumber) (StaticMemberExpression)

const parenthesizedMember = (user).name;
>     ^^^^^^^^^^^^^^^^^^^: string   (TyString) (BindingIdentifier)

const parenthesizedMember = (user).name;
>                            ^^^^: User   (TyTypeReference) (IdentifierReference)

const parenthesizedMember = (user).name;
>                                  ^^^^: string   (TyString) (StaticMemberExpression)

const assertedMember = (user as User).age;
>     ^^^^^^^^^^^^^^: number   (TyNumber) (BindingIdentifier)

const assertedMember = (user as User).age;
>                       ^^^^: User   (TyTypeReference) (IdentifierReference)

const assertedMember = (user as User).age;
>                               ^^^^: any   (TyAny) (TSTypeReference)

const assertedMember = (user as User).age;
>                                     ^^^: number   (TyNumber) (StaticMemberExpression)

const satisfiesMember = ({ value: 1 } satisfies { value: number }).value;
>     ^^^^^^^^^^^^^^^: number   (TyNumber) (BindingIdentifier)

const satisfiesMember = ({ value: 1 } satisfies { value: number }).value;
>                          ^^^^^: number   (TyNumber) (ObjectProperty)

const satisfiesMember = ({ value: 1 } satisfies { value: number }).value;
>                                                 ^^^^^: number   (TyNumber) (TSPropertySignature)

const satisfiesMember = ({ value: 1 } satisfies { value: number }).value;
>                                                                  ^^^^^: number   (TyNumber) (StaticMemberExpression)
