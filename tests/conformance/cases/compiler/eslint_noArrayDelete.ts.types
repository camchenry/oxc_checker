declare const arr: number[];
>             ^^^: number[]   (TyArray) (BindingIdentifier)

delete arr[0];
>^^^^^^^^^^^^^^: boolean   (TyBoolean) (ExpressionStatement)

delete arr[0];
>      ^^^: number[]   (TyArray) (IdentifierReference)

declare const arr: number[];
>             ^^^: number[]   (TyArray) (BindingIdentifier)

arr.splice(0, 1);
>^^^: number[]   (TyArray) (IdentifierReference)

arr.splice(0, 1);
>^^^^^^^^^^^^^^^^^: number[]   (TyArray) (ExpressionStatement)

arr.splice(0, 1);
>   ^^^^^^: { (start: number, deleteCount?: number): number[]; (start: number, deleteCount: number, ...items: number[]): number[]; }   (TyObject) (StaticMemberExpression)
