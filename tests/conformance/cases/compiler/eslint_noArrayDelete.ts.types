declare const arr: number[];
>             ^^^: number[]   (Array) (Identifier)

delete arr[0];
>^^^^^^^^^^^^^^: boolean   (Boolean) (ExpressionStatement)

delete arr[0];
>      ^^^: number[]   (Array) (Identifier)

declare const arr: number[];
>             ^^^: number[]   (Array) (Identifier)

arr.splice(0, 1);
>^^^: number[]   (Array) (Identifier)

arr.splice(0, 1);
>^^^^^^^^^^^^^^^^^: number[]   (Array) (ExpressionStatement)

arr.splice(0, 1);
>   ^^^^^^: { (start: number, deleteCount?: number): number[]; (start: number, deleteCount: number, ...items: number[]): number[]; }   (Object) (Identifier)
