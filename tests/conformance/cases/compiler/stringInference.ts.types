type User = {
>    ^^^^: { name: string; address: { city: string; }; }   (Object) (Identifier)

  name: string
> ^^^^: string   (String) (Identifier)

  address: {
> ^^^^^^^: { city: string; }   (Object) (Identifier)

    city: string
>   ^^^^: string   (String) (Identifier)

type UserKeys = keyof User
>    ^^^^^^^^: keyof User   (Keyof) (Identifier)

type UserKeys = keyof User
>                     ^^^^: any   (Any) (Identifier)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>    ^^^^^^^^: T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]   (Conditional) (Identifier)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>             ^: any   (Any) (Identifier)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>                                 ^: any   (Any) (Identifier)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>                                                    ^: any   (Any) (Identifier)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>                                                               ^: any   (Any) (Identifier)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>                                                                      ^: any   (Any) (Identifier)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>                                                                            ^^^^^^^^: any   (Any) (Identifier)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>                                                                                     ^: any   (Any) (Identifier)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>                                                                                            ^: any   (Any) (Identifier)

type Tuple1 = Tupleify<"test">
>    ^^^^^^: ["test"]   (Tuple) (Identifier)

type Tuple1 = Tupleify<"test">
>             ^^^^^^^^: any   (Any) (Identifier)

type Tuple2 = Tupleify<"test.property">
>    ^^^^^^: ["test", "property"]   (Tuple) (Identifier)

type Tuple2 = Tupleify<"test.property">
>             ^^^^^^^^: any   (Any) (Identifier)

type Tuple3 = Tupleify<"test.property.another">
>    ^^^^^^: ["test", "property", "another"]   (Tuple) (Identifier)

type Tuple3 = Tupleify<"test.property.another">
>             ^^^^^^^^: any   (Any) (Identifier)

type Tuple4 = Tupleify<".property.another">
>    ^^^^^^: ["", "property", "another"]   (Tuple) (Identifier)

type Tuple4 = Tupleify<".property.another">
>             ^^^^^^^^: any   (Any) (Identifier)

type T0 = "user" extends `${infer K}` ? K : never
>    ^^: "user"   (StringLiteral) (Identifier)

type T0 = "user" extends `${infer K}` ? K : never
>                                 ^: any   (Any) (Identifier)

type T0 = "user" extends `${infer K}` ? K : never
>                                       ^: any   (Any) (Identifier)

type T1 = "user.name" extends `${infer K}.${infer R}` ? true : false
>    ^^: true   (BooleanLiteral) (Identifier)

type T1 = "user.name" extends `${infer K}.${infer R}` ? true : false
>                                      ^: any   (Any) (Identifier)

type T1 = "user.name" extends `${infer K}.${infer R}` ? true : false
>                                                 ^: any   (Any) (Identifier)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>    ^^: { name: string; }   (Object) (Identifier)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                      ^: any   (Any) (Identifier)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                                 ^: any   (Any) (Identifier)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                                       ^: any   (Any) (Identifier)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                                                 ^^^^^^^^: any   (Any) (Identifier)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                                                            ^^^^: any   (Any) (Identifier)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                                                            ^^^^: any   (Error) (Identifier)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                                                                 ^^^^: any   (Any) (Identifier)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                                                                       ^: any   (Any) (Identifier)

type T3 = Tupleify<"user.address.city"> extends [infer P1, infer P2, infer P3]
>    ^^: { city: string; }   (Object) (Identifier)

type T3 = Tupleify<"user.address.city"> extends [infer P1, infer P2, infer P3]
>         ^^^^^^^^: any   (Any) (Identifier)

type T3 = Tupleify<"user.address.city"> extends [infer P1, infer P2, infer P3]
>                                                      ^^: any   (Any) (Identifier)

type T3 = Tupleify<"user.address.city"> extends [infer P1, infer P2, infer P3]
>                                                                ^^: any   (Any) (Identifier)

type T3 = Tupleify<"user.address.city"> extends [infer P1, infer P2, infer P3]
>                                                                          ^^: any   (Any) (Identifier)

  ? P2 extends UserKeys
>   ^^: any   (Any) (Identifier)

  ? P2 extends UserKeys
>              ^^^^^^^^: any   (Any) (Identifier)

    ? P3 extends keyof User[P2]
>     ^^: any   (Any) (Identifier)

    ? P3 extends keyof User[P2]
>                      ^^^^: any   (Any) (Identifier)

    ? P3 extends keyof User[P2]
>                           ^^: any   (Any) (Identifier)

      ? Pick<User[P2], P3>
>       ^^^^: any   (Any) (Identifier)

      ? Pick<User[P2], P3>
>       ^^^^: any   (Error) (Identifier)

      ? Pick<User[P2], P3>
>            ^^^^: any   (Any) (Identifier)

      ? Pick<User[P2], P3>
>                 ^^: any   (Any) (Identifier)

      ? Pick<User[P2], P3>
>                      ^^: any   (Any) (Identifier)
