type User = {
>    ^^^^: { name: string; address: { city: string; }; }   (TyObject) (TSTypeAliasDeclaration)

  name: string
> ^^^^: string   (TyString) (TSPropertySignature)

  address: {
> ^^^^^^^: { city: string; }   (TyObject) (TSPropertySignature)

    city: string
>   ^^^^: string   (TyString) (TSPropertySignature)

type UserKeys = keyof User
>    ^^^^^^^^: keyof User   (TyKeyof) (TSTypeAliasDeclaration)

type UserKeys = keyof User
>                     ^^^^: any   (TyAny) (TSTypeReference)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>    ^^^^^^^^: T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]   (TyConditional) (TSTypeAliasDeclaration)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>             ^: any   (TyAny) (TSTypeParameter)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>                                 ^: any   (TyAny) (TSTypeReference)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>                                                    ^: any   (TyAny) (TSTypeParameter)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>                                                               ^: any   (TyAny) (TSTypeParameter)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>                                                                      ^: any   (TyAny) (TSTypeReference)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>                                                                            ^^^^^^^^: any   (TyAny) (TSTypeReference)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>                                                                                     ^: any   (TyAny) (TSTypeReference)

type Tupleify<T extends string> = T extends `${infer K}.${infer R}` ? [K, ...Tupleify<R>] : [T]
>                                                                                            ^: any   (TyAny) (TSTypeReference)

type Tuple1 = Tupleify<"test">
>    ^^^^^^: ["test"]   (TyTuple) (TSTypeAliasDeclaration)

type Tuple1 = Tupleify<"test">
>             ^^^^^^^^: any   (TyAny) (TSTypeReference)

type Tuple2 = Tupleify<"test.property">
>    ^^^^^^: ["test", "property"]   (TyTuple) (TSTypeAliasDeclaration)

type Tuple2 = Tupleify<"test.property">
>             ^^^^^^^^: any   (TyAny) (TSTypeReference)

type Tuple3 = Tupleify<"test.property.another">
>    ^^^^^^: ["test", "property", "another"]   (TyTuple) (TSTypeAliasDeclaration)

type Tuple3 = Tupleify<"test.property.another">
>             ^^^^^^^^: any   (TyAny) (TSTypeReference)

type Tuple4 = Tupleify<".property.another">
>    ^^^^^^: ["", "property", "another"]   (TyTuple) (TSTypeAliasDeclaration)

type Tuple4 = Tupleify<".property.another">
>             ^^^^^^^^: any   (TyAny) (TSTypeReference)

type T0 = "user" extends `${infer K}` ? K : never
>    ^^: "user"   (TyStringLiteral) (TSTypeAliasDeclaration)

type T0 = "user" extends `${infer K}` ? K : never
>                                 ^: any   (TyAny) (TSTypeParameter)

type T0 = "user" extends `${infer K}` ? K : never
>                                       ^: any   (TyAny) (TSTypeReference)

type T1 = "user.name" extends `${infer K}.${infer R}` ? true : false
>    ^^: true   (TyBooleanLiteral) (TSTypeAliasDeclaration)

type T1 = "user.name" extends `${infer K}.${infer R}` ? true : false
>                                      ^: any   (TyAny) (TSTypeParameter)

type T1 = "user.name" extends `${infer K}.${infer R}` ? true : false
>                                                 ^: any   (TyAny) (TSTypeParameter)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>    ^^: { name: string; }   (TyObject) (TSTypeAliasDeclaration)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                      ^: any   (TyAny) (TSTypeParameter)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                                 ^: any   (TyAny) (TSTypeParameter)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                                       ^: any   (TyAny) (TSTypeReference)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                                                 ^^^^^^^^: any   (TyAny) (TSTypeReference)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                                                            ^^^^: any   (TyAny) (TSTypeReference)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                                                            ^^^^: any   (TyError) (IdentifierReference)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                                                                 ^^^^: any   (TyAny) (TSTypeReference)

type T2 = "user.name" extends `${infer K}.${infer R}` ? R extends UserKeys ? Pick<User, R> : never : never
>                                                                                       ^: any   (TyAny) (TSTypeReference)

type T3 = Tupleify<"user.address.city"> extends [infer P1, infer P2, infer P3]
>    ^^: { city: string; }   (TyObject) (TSTypeAliasDeclaration)

type T3 = Tupleify<"user.address.city"> extends [infer P1, infer P2, infer P3]
>         ^^^^^^^^: any   (TyAny) (TSTypeReference)

type T3 = Tupleify<"user.address.city"> extends [infer P1, infer P2, infer P3]
>                                                      ^^: any   (TyAny) (TSTypeParameter)

type T3 = Tupleify<"user.address.city"> extends [infer P1, infer P2, infer P3]
>                                                                ^^: any   (TyAny) (TSTypeParameter)

type T3 = Tupleify<"user.address.city"> extends [infer P1, infer P2, infer P3]
>                                                                          ^^: any   (TyAny) (TSTypeParameter)

  ? P2 extends UserKeys
>   ^^: any   (TyAny) (TSTypeReference)

  ? P2 extends UserKeys
>              ^^^^^^^^: any   (TyAny) (TSTypeReference)

    ? P3 extends keyof User[P2]
>     ^^: any   (TyAny) (TSTypeReference)

    ? P3 extends keyof User[P2]
>                      ^^^^: any   (TyAny) (TSTypeReference)

    ? P3 extends keyof User[P2]
>                           ^^: any   (TyAny) (TSTypeReference)

      ? Pick<User[P2], P3>
>       ^^^^: any   (TyAny) (TSTypeReference)

      ? Pick<User[P2], P3>
>       ^^^^: any   (TyError) (IdentifierReference)

      ? Pick<User[P2], P3>
>            ^^^^: any   (TyAny) (TSTypeReference)

      ? Pick<User[P2], P3>
>                 ^^: any   (TyAny) (TSTypeReference)

      ? Pick<User[P2], P3>
>                      ^^: any   (TyAny) (TSTypeReference)
