const ab1 = `${'a'}${'b'}`;
>     ^^^: "ab"   (TyStringLiteral)

const ab2 = `a${'b'}`;
>     ^^^: "ab"   (TyStringLiteral)

type AB1 = `${'A'}${'B'}`;
>    ^^^: `${"A"}${"B"}`   (TyTemplateLiteral)

type AB2 = `A${'B'}`;
>    ^^^: `A${"B"}`   (TyTemplateLiteral)

const stringWithNumber = `${'1 + 1 = '}${2}`;
>     ^^^^^^^^^^^^^^^^: "1 + 1 = 2"   (TyStringLiteral)

const stringWithBoolean = `${'true is '}${true}`;
>     ^^^^^^^^^^^^^^^^^: "true is true"   (TyStringLiteral)

const text = 'a';
>     ^^^^: "a"   (TyStringLiteral)

const wrappedText = `${text}`;
>     ^^^^^^^^^^^: "a"   (TyStringLiteral)

const wrappedText = `${text}`;
>                      ^^^^: "a"   (TyStringLiteral)

type Text = 'A';
>    ^^^^: "A"   (TyStringLiteral)

type WrappedText = `${Text}`;
>    ^^^^^^^^^^^: `${Text}`   (TyTemplateLiteral)

type WrappedText = `${Text}`;
>                     ^^^^: any   (TyAny)

declare const intersectionWithString: string & { _brand: 'test-brand' };
>             ^^^^^^^^^^^^^^^^^^^^^^: string & { _brand: "test-brand"; }   (TyIntersection)

declare const intersectionWithString: string & { _brand: 'test-brand' };
>                                                ^^^^^^: "test-brand"   (TyStringLiteral)

const wrappedIntersection = `${intersectionWithString}`;
>     ^^^^^^^^^^^^^^^^^^^: string   (TyString)

const wrappedIntersection = `${intersectionWithString}`;
>                              ^^^^^^^^^^^^^^^^^^^^^^: string & { _brand: "test-brand"; }   (TyIntersection)

type IntersectionWithString = string & { _brand: 'test-brand' };
>    ^^^^^^^^^^^^^^^^^^^^^^: string & { _brand: "test-brand"; }   (TyIntersection)

type IntersectionWithString = string & { _brand: 'test-brand' };
>                                        ^^^^^^: "test-brand"   (TyStringLiteral)

type WrappedIntersection = `${IntersectionWithString}`;
>    ^^^^^^^^^^^^^^^^^^^: `${IntersectionWithString}`   (TyTemplateLiteral)

type WrappedIntersection = `${IntersectionWithString}`;
>                             ^^^^^^^^^^^^^^^^^^^^^^: any   (TyAny)

const ab1 = `ab`;
>     ^^^: "ab"   (TyStringLiteral)

const ab2 = `ab`;
>     ^^^: "ab"   (TyStringLiteral)

type AB = `AB`;
>    ^^: `AB`   (TyTemplateLiteral)

type ABCUnion = `${ABC}`;
>    ^^^^^^^^: `${ABC}`   (TyTemplateLiteral)

type ABCUnion = `${ABC}`;
>                  ^^^: any   (TyAny)

type A = `${ABC.A}`;
>    ^: `${ABC.A}`   (TyTemplateLiteral)

type A = `${ABC.A}`;
>           ^^^^^: any   (TyAny)

type TextUtil<T extends string> = `${T}`;
>    ^^^^^^^^: `${T}`   (TyTemplateLiteral)

type TextUtil<T extends string> = `${T}`;
>             ^: any   (TyAny)

type TextUtil<T extends string> = `${T}`;
>                                    ^: any   (TyAny)

const stringWithNumber = `1 + 1 = 2`;
>     ^^^^^^^^^^^^^^^^: "1 + 1 = 2"   (TyStringLiteral)

const stringWithBoolean = `true is true`;
>     ^^^^^^^^^^^^^^^^^: "true is true"   (TyStringLiteral)

const text = 'a';
>     ^^^^: "a"   (TyStringLiteral)

const wrappedText = text;
>     ^^^^^^^^^^^: "a"   (TyStringLiteral)

const wrappedText = text;
>                   ^^^^: "a"   (TyStringLiteral)

type Text = 'A';
>    ^^^^: "A"   (TyStringLiteral)

type WrappedText = Text;
>    ^^^^^^^^^^^: "A"   (TyStringLiteral)

type WrappedText = Text;
>                  ^^^^: any   (TyAny)

declare const intersectionWithString: string & { _brand: 'test-brand' };
>             ^^^^^^^^^^^^^^^^^^^^^^: string & { _brand: "test-brand"; }   (TyIntersection)

declare const intersectionWithString: string & { _brand: 'test-brand' };
>                                                ^^^^^^: "test-brand"   (TyStringLiteral)

const wrappedIntersection = intersectionWithString;
>     ^^^^^^^^^^^^^^^^^^^: string & { _brand: "test-brand"; }   (TyIntersection)

const wrappedIntersection = intersectionWithString;
>                           ^^^^^^^^^^^^^^^^^^^^^^: string & { _brand: "test-brand"; }   (TyIntersection)

type IntersectionWithString = string & { _brand: 'test-brand' };
>    ^^^^^^^^^^^^^^^^^^^^^^: string & { _brand: "test-brand"; }   (TyIntersection)

type IntersectionWithString = string & { _brand: 'test-brand' };
>                                        ^^^^^^: "test-brand"   (TyStringLiteral)

type WrappedIntersection = IntersectionWithString;
>    ^^^^^^^^^^^^^^^^^^^: string & { _brand: "test-brand"; }   (TyIntersection)

type WrappedIntersection = IntersectionWithString;
>                          ^^^^^^^^^^^^^^^^^^^^^^: any   (TyAny)
