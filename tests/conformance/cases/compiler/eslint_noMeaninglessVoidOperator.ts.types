void (() => {})();
>^^^^^^^^^^^^^^^^^^: undefined   (TyUndefined) (ExpressionStatement)

function foo() {}
>        ^^^: () => void   (TyFunction) (BindingIdentifier)

void foo();
>^^^^^^^^^^^: undefined   (TyUndefined) (ExpressionStatement)

void foo();
>    ^^^: () => void   (TyFunction) (IdentifierReference)

(() => {})();
>^^^^^^^^^^^^^: void   (TyVoid) (ExpressionStatement)

function foo() {}
>        ^^^: () => void   (TyFunction) (BindingIdentifier)

foo(); // nothing to discard
>^^^: () => void   (TyFunction) (IdentifierReference)

foo(); // nothing to discard
>^^^^^^: void   (TyVoid) (ExpressionStatement)

function bar(x: number) {
>        ^^^: (x: number) => number   (TyFunction) (BindingIdentifier)

function bar(x: number) {
>            ^: number   (TyNumber) (BindingIdentifier)

  void x; // discarding a number
> ^^^^^^^: undefined   (TyUndefined) (ExpressionStatement)

  void x; // discarding a number
>      ^: number   (TyNumber) (IdentifierReference)

void bar(1); // discarding a number
>^^^^^^^^^^^^: undefined   (TyUndefined) (ExpressionStatement)

void bar(1); // discarding a number
>    ^^^: (x: number) => number   (TyFunction) (IdentifierReference)
