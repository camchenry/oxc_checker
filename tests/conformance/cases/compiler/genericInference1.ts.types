['a', 'b', 'c'].map(x => x.length);
>^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^: any[]   (TyArray)

['a', 'b', 'c'].map(x => x.length);
>               ^^^: <U>(callbackfn: (value: string, index: number, array: string[]) => U, thisArg?: any) => U[]   (TyFunction)

['a', 'b', 'c'].map(x => x.length);
>                   ^: string   (TyString)

['a', 'b', 'c'].map(x => x.length);
>                        ^: string   (TyString)

['a', 'b', 'c'].map(x => x.length);
>                        ^^^^^^^^: any   (TyAny)

['a', 'b', 'c'].map(x => x.length);
>                          ^^^^^^: any   (TyAny)
