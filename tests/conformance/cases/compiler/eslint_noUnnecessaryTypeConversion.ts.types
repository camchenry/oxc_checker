String('123');
>^^^^^^: StringConstructor   (TypeReference) (Identifier)

String('123');
>^^^^^^^^^^^^^^: string   (String) (ExpressionStatement)

'123'.toString();
>^^^^^^^^^^^^^^^^^: string   (String) (ExpressionStatement)

'123'.toString();
>     ^^^^^^^^: () => string   (Function) (Identifier)

'' + '123';
>^^^^^^^^^^^: string   (String) (ExpressionStatement)

'123' + '';
>^^^^^^^^^^^: string   (String) (ExpressionStatement)

Number(123);
>^^^^^^: NumberConstructor   (TypeReference) (Identifier)

Number(123);
>^^^^^^^^^^^^: number   (Number) (ExpressionStatement)

+123;
>^^^^^: 123   (NumberLiteral) (ExpressionStatement)

~~123;
>^^^^^^: number   (Number) (ExpressionStatement)

Boolean(true);
>^^^^^^^: BooleanConstructor   (TypeReference) (Identifier)

Boolean(true);
>^^^^^^^^^^^^^^: boolean   (Boolean) (ExpressionStatement)

!!true;
>^^^^^^^: true   (BooleanLiteral) (ExpressionStatement)

BigInt(BigInt(1));
>^^^^^^: BigIntConstructor   (TypeReference) (Identifier)

BigInt(BigInt(1));
>^^^^^^^^^^^^^^^^^^: bigint   (Bigint) (ExpressionStatement)

BigInt(BigInt(1));
>      ^^^^^^: BigIntConstructor   (TypeReference) (Identifier)

let str = '123';
>   ^^^: string   (String) (Identifier)

str += '';
>^^^: string   (String) (Identifier)

str += '';
>^^^^^^^^^^: string   (String) (ExpressionStatement)

function foo(bar: string | number) {
>        ^^^: (bar: string | number) => void   (Function) (Identifier)

function foo(bar: string | number) {
>            ^^^: string | number   (Union) (Identifier)

  String(bar);
> ^^^^^^: StringConstructor   (TypeReference) (Identifier)

  String(bar);
> ^^^^^^^^^^^^: string   (String) (ExpressionStatement)

  String(bar);
>        ^^^: string | number   (Union) (Identifier)

  bar.toString();
> ^^^: string | number   (Union) (Identifier)

  bar.toString();
> ^^^^^^^^^^^^^^^: string   (String) (ExpressionStatement)

  bar.toString();
>     ^^^^^^^^: (() => string) | ((radix?: number) => string)   (Union) (Identifier)

  '' + bar;
> ^^^^^^^^^: string   (String) (ExpressionStatement)

  '' + bar;
>      ^^^: string | number   (Union) (Identifier)

  bar + '';
> ^^^: string | number   (Union) (Identifier)

  bar + '';
> ^^^^^^^^^: string   (String) (ExpressionStatement)

  Number(bar);
> ^^^^^^: NumberConstructor   (TypeReference) (Identifier)

  Number(bar);
> ^^^^^^^^^^^^: number   (Number) (ExpressionStatement)

  Number(bar);
>        ^^^: string | number   (Union) (Identifier)

  +bar;
> ^^^^^: number   (Number) (ExpressionStatement)

  +bar;
>  ^^^: string | number   (Union) (Identifier)

  ~~bar;
> ^^^^^^: number   (Number) (ExpressionStatement)

  ~~bar;
>   ^^^: string | number   (Union) (Identifier)

  Boolean(bar);
> ^^^^^^^: BooleanConstructor   (TypeReference) (Identifier)

  Boolean(bar);
> ^^^^^^^^^^^^^: boolean   (Boolean) (ExpressionStatement)

  Boolean(bar);
>         ^^^: string | number   (Union) (Identifier)

  !!bar;
> ^^^^^^: boolean   (Boolean) (ExpressionStatement)

  !!bar;
>   ^^^: string | number   (Union) (Identifier)

  BigInt(1);
> ^^^^^^: BigIntConstructor   (TypeReference) (Identifier)

  BigInt(1);
> ^^^^^^^^^^: bigint   (Bigint) (ExpressionStatement)

  bar += '';
> ^^^: string | number   (Union) (Identifier)

  bar += '';
> ^^^^^^^^^^: string   (String) (ExpressionStatement)
