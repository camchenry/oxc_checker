String('123');
>^^^^^^: StringConstructor   (TyTypeReference) (IdentifierReference)

String('123');
>^^^^^^^^^^^^^^: string   (TyString) (ExpressionStatement)

'123'.toString();
>^^^^^^^^^^^^^^^^^: string   (TyString) (ExpressionStatement)

'123'.toString();
>     ^^^^^^^^: () => string   (TyFunction) (StaticMemberExpression)

'' + '123';
>^^^^^^^^^^^: string   (TyString) (ExpressionStatement)

'123' + '';
>^^^^^^^^^^^: string   (TyString) (ExpressionStatement)

Number(123);
>^^^^^^: NumberConstructor   (TyTypeReference) (IdentifierReference)

Number(123);
>^^^^^^^^^^^^: number   (TyNumber) (ExpressionStatement)

+123;
>^^^^^: 123   (TyNumberLiteral) (ExpressionStatement)

~~123;
>^^^^^^: number   (TyNumber) (ExpressionStatement)

Boolean(true);
>^^^^^^^: BooleanConstructor   (TyTypeReference) (IdentifierReference)

Boolean(true);
>^^^^^^^^^^^^^^: boolean   (TyBoolean) (ExpressionStatement)

!!true;
>^^^^^^^: true   (TyBooleanLiteral) (ExpressionStatement)

BigInt(BigInt(1));
>^^^^^^: BigIntConstructor   (TyTypeReference) (IdentifierReference)

BigInt(BigInt(1));
>^^^^^^^^^^^^^^^^^^: bigint   (TyBigint) (ExpressionStatement)

BigInt(BigInt(1));
>      ^^^^^^: BigIntConstructor   (TyTypeReference) (IdentifierReference)

let str = '123';
>   ^^^: string   (TyString) (BindingIdentifier)

str += '';
>^^^: string   (TyString) (IdentifierReference)

str += '';
>^^^^^^^^^^: string   (TyString) (ExpressionStatement)

function foo(bar: string | number) {
>        ^^^: (bar: string | number) => void   (TyFunction) (BindingIdentifier)

function foo(bar: string | number) {
>            ^^^: string | number   (TyUnion) (BindingIdentifier)

  String(bar);
> ^^^^^^: StringConstructor   (TyTypeReference) (IdentifierReference)

  String(bar);
> ^^^^^^^^^^^^: string   (TyString) (ExpressionStatement)

  String(bar);
>        ^^^: string | number   (TyUnion) (IdentifierReference)

  bar.toString();
> ^^^: string | number   (TyUnion) (IdentifierReference)

  bar.toString();
> ^^^^^^^^^^^^^^^: string   (TyString) (ExpressionStatement)

  bar.toString();
>     ^^^^^^^^: (() => string) | ((radix?: number) => string)   (TyUnion) (StaticMemberExpression)

  '' + bar;
> ^^^^^^^^^: string   (TyString) (ExpressionStatement)

  '' + bar;
>      ^^^: string | number   (TyUnion) (IdentifierReference)

  bar + '';
> ^^^: string | number   (TyUnion) (IdentifierReference)

  bar + '';
> ^^^^^^^^^: string   (TyString) (ExpressionStatement)

  Number(bar);
> ^^^^^^: NumberConstructor   (TyTypeReference) (IdentifierReference)

  Number(bar);
> ^^^^^^^^^^^^: number   (TyNumber) (ExpressionStatement)

  Number(bar);
>        ^^^: string | number   (TyUnion) (IdentifierReference)

  +bar;
> ^^^^^: number   (TyNumber) (ExpressionStatement)

  +bar;
>  ^^^: string | number   (TyUnion) (IdentifierReference)

  ~~bar;
> ^^^^^^: number   (TyNumber) (ExpressionStatement)

  ~~bar;
>   ^^^: string | number   (TyUnion) (IdentifierReference)

  Boolean(bar);
> ^^^^^^^: BooleanConstructor   (TyTypeReference) (IdentifierReference)

  Boolean(bar);
> ^^^^^^^^^^^^^: boolean   (TyBoolean) (ExpressionStatement)

  Boolean(bar);
>         ^^^: string | number   (TyUnion) (IdentifierReference)

  !!bar;
> ^^^^^^: boolean   (TyBoolean) (ExpressionStatement)

  !!bar;
>   ^^^: string | number   (TyUnion) (IdentifierReference)

  BigInt(1);
> ^^^^^^: BigIntConstructor   (TyTypeReference) (IdentifierReference)

  BigInt(1);
> ^^^^^^^^^^: bigint   (TyBigint) (ExpressionStatement)

  bar += '';
> ^^^: string | number   (TyUnion) (IdentifierReference)

  bar += '';
> ^^^^^^^^^^: string   (TyString) (ExpressionStatement)
