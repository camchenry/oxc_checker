declare const array: string[];
>             ^^^^^: string[]   (TyArray) (BindingIdentifier)

for (const i in array) {
>          ^: string   (TyString) (BindingIdentifier)

for (const i in array) {
>               ^^^^^: string[]   (TyArray) (IdentifierReference)

  console.log(array[i]);
> ^^^^^^^: Console   (TyTypeReference) (IdentifierReference)

  console.log(array[i]);
> ^^^^^^^^^^^^^^^^^^^^^^: void   (TyVoid) (ExpressionStatement)

  console.log(array[i]);
>         ^^^: (...data: any[]) => void   (TyFunction) (StaticMemberExpression)

  console.log(array[i]);
>             ^^^^^: string[]   (TyArray) (IdentifierReference)

  console.log(array[i]);
>                   ^: string   (TyString) (IdentifierReference)

for (const i in array) {
>          ^: string   (TyString) (BindingIdentifier)

for (const i in array) {
>               ^^^^^: string[]   (TyArray) (IdentifierReference)

  console.log(i, array[i]);
> ^^^^^^^: Console   (TyTypeReference) (IdentifierReference)

  console.log(i, array[i]);
> ^^^^^^^^^^^^^^^^^^^^^^^^^: void   (TyVoid) (ExpressionStatement)

  console.log(i, array[i]);
>         ^^^: (...data: any[]) => void   (TyFunction) (StaticMemberExpression)

  console.log(i, array[i]);
>             ^: string   (TyString) (IdentifierReference)

  console.log(i, array[i]);
>                ^^^^^: string[]   (TyArray) (IdentifierReference)

  console.log(i, array[i]);
>                      ^: string   (TyString) (IdentifierReference)

declare const array: string[];
>             ^^^^^: string[]   (TyArray) (BindingIdentifier)

for (const value of array) {
>          ^^^^^: string   (TyString) (BindingIdentifier)

for (const value of array) {
>                   ^^^^^: string[]   (TyArray) (IdentifierReference)

  console.log(value);
> ^^^^^^^: Console   (TyTypeReference) (IdentifierReference)

  console.log(value);
> ^^^^^^^^^^^^^^^^^^^: void   (TyVoid) (ExpressionStatement)

  console.log(value);
>         ^^^: (...data: any[]) => void   (TyFunction) (StaticMemberExpression)

  console.log(value);
>             ^^^^^: string   (TyString) (IdentifierReference)

for (let i = 0; i < array.length; i += 1) {
>        ^: number   (TyNumber) (BindingIdentifier)

for (let i = 0; i < array.length; i += 1) {
>               ^: number   (TyNumber) (IdentifierReference)

for (let i = 0; i < array.length; i += 1) {
>                   ^^^^^: string[]   (TyArray) (IdentifierReference)

for (let i = 0; i < array.length; i += 1) {
>                         ^^^^^^: number   (TyNumber) (StaticMemberExpression)

for (let i = 0; i < array.length; i += 1) {
>                                 ^: number   (TyNumber) (IdentifierReference)

  console.log(i, array[i]);
> ^^^^^^^: Console   (TyTypeReference) (IdentifierReference)

  console.log(i, array[i]);
> ^^^^^^^^^^^^^^^^^^^^^^^^^: void   (TyVoid) (ExpressionStatement)

  console.log(i, array[i]);
>         ^^^: (...data: any[]) => void   (TyFunction) (StaticMemberExpression)

  console.log(i, array[i]);
>             ^: number   (TyNumber) (IdentifierReference)

  console.log(i, array[i]);
>                ^^^^^: string[]   (TyArray) (IdentifierReference)

  console.log(i, array[i]);
>                      ^: number   (TyNumber) (IdentifierReference)

array.forEach((value, i) => {
>^^^^^: string[]   (TyArray) (IdentifierReference)

array.forEach((value, i) => {
>^^^^^^^^^^^^^^^^^^^^^^^^^^^^^: void   (TyVoid) (ExpressionStatement)

array.forEach((value, i) => {
>     ^^^^^^^: (callbackfn: (value: string, index: number, array: string[]) => void, thisArg?: any) => void   (TyFunction) (StaticMemberExpression)

array.forEach((value, i) => {
>              ^^^^^: string   (TyString) (BindingIdentifier)

array.forEach((value, i) => {
>                     ^: number   (TyNumber) (BindingIdentifier)

  console.log(i, value);
> ^^^^^^^: Console   (TyTypeReference) (IdentifierReference)

  console.log(i, value);
> ^^^^^^^^^^^^^^^^^^^^^^: void   (TyVoid) (ExpressionStatement)

  console.log(i, value);
>         ^^^: (...data: any[]) => void   (TyFunction) (StaticMemberExpression)

  console.log(i, value);
>             ^: number   (TyNumber) (IdentifierReference)

  console.log(i, value);
>                ^^^^^: string   (TyString) (IdentifierReference)

for (const [i, value] of array.entries()) {
>           ^: [number, string]   (TyTuple) (BindingIdentifier)
     expected: number

for (const [i, value] of array.entries()) {
>              ^^^^^: [number, string]   (TyTuple) (BindingIdentifier)
            expected: string

for (const [i, value] of array.entries()) {
>                        ^^^^^: string[]   (TyArray) (IdentifierReference)

for (const [i, value] of array.entries()) {
>                              ^^^^^^^: () => ArrayIterator<[number, string]>   (TyFunction) (StaticMemberExpression)

  console.log(i, value);
> ^^^^^^^: Console   (TyTypeReference) (IdentifierReference)

  console.log(i, value);
> ^^^^^^^^^^^^^^^^^^^^^^: void   (TyVoid) (ExpressionStatement)

  console.log(i, value);
>         ^^^: (...data: any[]) => void   (TyFunction) (StaticMemberExpression)

  console.log(i, value);
>             ^: [number, string]   (TyTuple) (IdentifierReference)
       expected: number

  console.log(i, value);
>                ^^^^^: [number, string]   (TyTuple) (IdentifierReference)
              expected: string
