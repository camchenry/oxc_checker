function makeActionCreator(type: string) {
>        ^^^^^^^^^^^^^^^^^: (type: string) => { (payload: string): { type: string; payload: string; }; type: string; match: (value: unknown) => value is { type: string; }; }   (Function) (Identifier)

function makeActionCreator(type: string) {
>                          ^^^^: string   (String) (Identifier)

    function actionCreator(payload: string) {
>            ^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match: (value: unknown) => value is { type: string; }; }   (Object) (Identifier)

    function actionCreator(payload: string) {
>                          ^^^^^^^: string   (String) (Identifier)

        return { type, payload };
>                ^^^^: string   (String) (Identifier)

        return { type, payload };
>                ^^^^: string   (String) (Identifier)

        return { type, payload };
>                      ^^^^^^^: string   (String) (Identifier)

        return { type, payload };
>                      ^^^^^^^: string   (String) (Identifier)

    actionCreator.type = type;
>   ^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match: (value: unknown) => value is { type: string; }; }   (Object) (Identifier)

    actionCreator.type = type;
>   ^^^^^^^^^^^^^^^^^^^^^^^^^^: string   (String) (ExpressionStatement)

    actionCreator.type = type;
>                 ^^^^: string   (String) (Identifier)

    actionCreator.type = type;
>                        ^^^^: string   (String) (Identifier)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>   ^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match: (value: unknown) => value is { type: string; }; }   (Object) (Identifier)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^: (value: unknown) => value is { type: string; }   (Function) (ExpressionStatement)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>                 ^^^^^: (value: unknown) => value is { type: string; }   (Function) (Identifier)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>                          ^^^^^: unknown   (Unknown) (Identifier)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>                                           ^^^^^: unknown   (Unknown) (Identifier)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>                                                      ^^^^: string   (String) (Identifier)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>                                                                               ^^^^^: unknown   (Unknown) (Identifier)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>                                                                                                     ^^^^^: object | null   (Union) (Identifier)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>                                                                                                                                 ^^^^^: object   (PrimitiveObject) (Identifier)

    const creatorType = actionCreator.type;
>         ^^^^^^^^^^^: string   (String) (Identifier)

    const creatorType = actionCreator.type;
>                       ^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match: (value: unknown) => value is { type: string; }; }   (Object) (Identifier)

    const creatorType = actionCreator.type;
>                                     ^^^^: string   (String) (Identifier)

    const matcher = actionCreator.match;
>         ^^^^^^^: (value: unknown) => value is { type: string; }   (Function) (Identifier)

    const matcher = actionCreator.match;
>                   ^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match: (value: unknown) => value is { type: string; }; }   (Object) (Identifier)

    const matcher = actionCreator.match;
>                                 ^^^^^: (value: unknown) => value is { type: string; }   (Function) (Identifier)

    const result = actionCreator("value");
>         ^^^^^^: { type: string; payload: string; }   (Object) (Identifier)

    const result = actionCreator("value");
>                  ^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match: (value: unknown) => value is { type: string; }; }   (Object) (Identifier)

    return actionCreator;
>          ^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match: (value: unknown) => value is { type: string; }; }   (Object) (Identifier)

const returnedCreator = makeActionCreator("demo");
>     ^^^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match: (value: unknown) => value is { type: string; }; }   (Object) (Identifier)

const returnedCreator = makeActionCreator("demo");
>                       ^^^^^^^^^^^^^^^^^: (type: string) => { (payload: string): { type: string; payload: string; }; type: string; match: (value: unknown) => value is { type: string; }; }   (Function) (Identifier)

const returnedType = returnedCreator.type;
>     ^^^^^^^^^^^^: string   (String) (Identifier)

const returnedType = returnedCreator.type;
>                    ^^^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match: (value: unknown) => value is { type: string; }; }   (Object) (Identifier)

const returnedType = returnedCreator.type;
>                                    ^^^^: string   (String) (Identifier)

const returnedMatch = returnedCreator.match({ type: "demo" });
>     ^^^^^^^^^^^^^: boolean   (Boolean) (Identifier)

const returnedMatch = returnedCreator.match({ type: "demo" });
>                     ^^^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match: (value: unknown) => value is { type: string; }; }   (Object) (Identifier)

const returnedMatch = returnedCreator.match({ type: "demo" });
>                                     ^^^^^: (value: unknown) => value is { type: string; }   (Function) (Identifier)

const returnedMatch = returnedCreator.match({ type: "demo" });
>                                             ^^^^: string   (String) (Identifier)

const returnedResult = returnedCreator("payload");
>     ^^^^^^^^^^^^^^: { type: string; payload: string; }   (Object) (Identifier)

const returnedResult = returnedCreator("payload");
>                      ^^^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match: (value: unknown) => value is { type: string; }; }   (Object) (Identifier)

const arrowCreator = (payload: number) => ({ payload });
>     ^^^^^^^^^^^^: { (payload: number): { payload: number; }; type: string; match: (value: unknown) => value is { payload: number; }; }   (Object) (Identifier)

const arrowCreator = (payload: number) => ({ payload });
>                     ^^^^^^^: number   (Number) (Identifier)

const arrowCreator = (payload: number) => ({ payload });
>                                            ^^^^^^^: number   (Number) (Identifier)

const arrowCreator = (payload: number) => ({ payload });
>                                            ^^^^^^^: number   (Number) (Identifier)

let arrowLabel = "arrow";
>   ^^^^^^^^^^: string   (String) (Identifier)

arrowCreator.type = arrowLabel;
>^^^^^^^^^^^^: { (payload: number): { payload: number; }; type: string; match: (value: unknown) => value is { payload: number; }; }   (Object) (Identifier)

arrowCreator.type = arrowLabel;
>^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^: string   (String) (ExpressionStatement)

arrowCreator.type = arrowLabel;
>            ^^^^: string   (String) (Identifier)

arrowCreator.type = arrowLabel;
>                   ^^^^^^^^^^: string   (String) (Identifier)

arrowCreator.match = (value: unknown): value is { payload: number } => true;
>^^^^^^^^^^^^: { (payload: number): { payload: number; }; type: string; match: (value: unknown) => value is { payload: number; }; }   (Object) (Identifier)

arrowCreator.match = (value: unknown): value is { payload: number } => true;
>^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^: (value: unknown) => value is { payload: number; }   (Function) (ExpressionStatement)

arrowCreator.match = (value: unknown): value is { payload: number } => true;
>            ^^^^^: (value: unknown) => value is { payload: number; }   (Function) (Identifier)

arrowCreator.match = (value: unknown): value is { payload: number } => true;
>                     ^^^^^: unknown   (Unknown) (Identifier)

arrowCreator.match = (value: unknown): value is { payload: number } => true;
>                                      ^^^^^: unknown   (Unknown) (Identifier)

arrowCreator.match = (value: unknown): value is { payload: number } => true;
>                                                 ^^^^^^^: number   (Number) (Identifier)

const arrowType = arrowCreator.type;
>     ^^^^^^^^^: string   (String) (Identifier)

const arrowType = arrowCreator.type;
>                 ^^^^^^^^^^^^: { (payload: number): { payload: number; }; type: string; match: (value: unknown) => value is { payload: number; }; }   (Object) (Identifier)

const arrowType = arrowCreator.type;
>                              ^^^^: string   (String) (Identifier)

const arrowMatch = arrowCreator.match;
>     ^^^^^^^^^^: (value: unknown) => value is { payload: number; }   (Function) (Identifier)

const arrowMatch = arrowCreator.match;
>                  ^^^^^^^^^^^^: { (payload: number): { payload: number; }; type: string; match: (value: unknown) => value is { payload: number; }; }   (Object) (Identifier)

const arrowMatch = arrowCreator.match;
>                               ^^^^^: (value: unknown) => value is { payload: number; }   (Function) (Identifier)

const arrowResult = arrowCreator(1);
>     ^^^^^^^^^^^: { payload: number; }   (Object) (Identifier)

const arrowResult = arrowCreator(1);
>                   ^^^^^^^^^^^^: { (payload: number): { payload: number; }; type: string; match: (value: unknown) => value is { payload: number; }; }   (Object) (Identifier)
