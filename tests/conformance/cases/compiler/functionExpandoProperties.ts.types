function makeActionCreator(type: string) {
>        ^^^^^^^^^^^^^^^^^: (type: string) => { (payload: string): { type: string; payload: string; }; type: string; match(value: unknown): value is { type: string; }; }   (TyFunction) (BindingIdentifier)

function makeActionCreator(type: string) {
>                          ^^^^: string   (TyString) (BindingIdentifier)

    function actionCreator(payload: string) {
>            ^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match(value: unknown): value is { type: string; }; }   (TyObject) (BindingIdentifier)

    function actionCreator(payload: string) {
>                          ^^^^^^^: string   (TyString) (BindingIdentifier)

        return { type, payload };
>                ^^^^: string   (TyString) (ObjectProperty)

        return { type, payload };
>                ^^^^: string   (TyString) (IdentifierReference)

        return { type, payload };
>                      ^^^^^^^: string   (TyString) (ObjectProperty)

        return { type, payload };
>                      ^^^^^^^: string   (TyString) (IdentifierReference)

    actionCreator.type = type;
>   ^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match(value: unknown): value is { type: string; }; }   (TyObject) (IdentifierReference)

    actionCreator.type = type;
>   ^^^^^^^^^^^^^^^^^^^^^^^^^^: string   (TyString) (ExpressionStatement)

    actionCreator.type = type;
>                 ^^^^: string   (TyString) (StaticMemberExpression)

    actionCreator.type = type;
>                        ^^^^: string   (TyString) (IdentifierReference)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>   ^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match(value: unknown): value is { type: string; }; }   (TyObject) (IdentifierReference)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^: (value: unknown) => value is { type: string; }   (TyFunction) (ExpressionStatement)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>                 ^^^^^: (value: unknown) => value is { type: string; }   (TyFunction) (StaticMemberExpression)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>                          ^^^^^: unknown   (TyUnknown) (BindingIdentifier)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>                                           ^^^^^: unknown   (TyUnknown) (IdentifierName)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>                                                      ^^^^: string   (TyString) (TSPropertySignature)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>                                                                               ^^^^^: unknown   (TyUnknown) (IdentifierReference)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>                                                                                                     ^^^^^: object | null   (TyUnion) (IdentifierReference)

    actionCreator.match = (value: unknown): value is { type: string } => typeof value === "object" && value !== null && "type" in value;
>                                                                                                                                 ^^^^^: object   (TyPrimitiveObject) (IdentifierReference)

    const creatorType = actionCreator.type;
>         ^^^^^^^^^^^: string   (TyString) (BindingIdentifier)

    const creatorType = actionCreator.type;
>                       ^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match(value: unknown): value is { type: string; }; }   (TyObject) (IdentifierReference)

    const creatorType = actionCreator.type;
>                                     ^^^^: string   (TyString) (StaticMemberExpression)

    const matcher = actionCreator.match;
>         ^^^^^^^: (value: unknown) => value is { type: string; }   (TyFunction) (BindingIdentifier)

    const matcher = actionCreator.match;
>                   ^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match(value: unknown): value is { type: string; }; }   (TyObject) (IdentifierReference)

    const matcher = actionCreator.match;
>                                 ^^^^^: (value: unknown) => value is { type: string; }   (TyFunction) (StaticMemberExpression)

    const result = actionCreator("value");
>         ^^^^^^: { type: string; payload: string; }   (TyObject) (BindingIdentifier)

    const result = actionCreator("value");
>                  ^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match(value: unknown): value is { type: string; }; }   (TyObject) (IdentifierReference)

    return actionCreator;
>          ^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match(value: unknown): value is { type: string; }; }   (TyObject) (IdentifierReference)

const returnedCreator = makeActionCreator("demo");
>     ^^^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match(value: unknown): value is { type: string; }; }   (TyObject) (BindingIdentifier)

const returnedCreator = makeActionCreator("demo");
>                       ^^^^^^^^^^^^^^^^^: (type: string) => { (payload: string): { type: string; payload: string; }; type: string; match(value: unknown): value is { type: string; }; }   (TyFunction) (IdentifierReference)

const returnedType = returnedCreator.type;
>     ^^^^^^^^^^^^: string   (TyString) (BindingIdentifier)

const returnedType = returnedCreator.type;
>                    ^^^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match(value: unknown): value is { type: string; }; }   (TyObject) (IdentifierReference)

const returnedType = returnedCreator.type;
>                                    ^^^^: string   (TyString) (StaticMemberExpression)

const returnedMatch = returnedCreator.match({ type: "demo" });
>     ^^^^^^^^^^^^^: boolean   (TyBoolean) (BindingIdentifier)

const returnedMatch = returnedCreator.match({ type: "demo" });
>                     ^^^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match(value: unknown): value is { type: string; }; }   (TyObject) (IdentifierReference)

const returnedMatch = returnedCreator.match({ type: "demo" });
>                                     ^^^^^: (value: unknown) => value is { type: string; }   (TyFunction) (StaticMemberExpression)

const returnedMatch = returnedCreator.match({ type: "demo" });
>                                             ^^^^: string   (TyString) (ObjectProperty)

const returnedResult = returnedCreator("payload");
>     ^^^^^^^^^^^^^^: { type: string; payload: string; }   (TyObject) (BindingIdentifier)

const returnedResult = returnedCreator("payload");
>                      ^^^^^^^^^^^^^^^: { (payload: string): { type: string; payload: string; }; type: string; match(value: unknown): value is { type: string; }; }   (TyObject) (IdentifierReference)

const arrowCreator = (payload: number) => ({ payload });
>     ^^^^^^^^^^^^: { (payload: number): { payload: number; }; type: string; match(value: unknown): value is { payload: number; }; }   (TyObject) (BindingIdentifier)

const arrowCreator = (payload: number) => ({ payload });
>                     ^^^^^^^: number   (TyNumber) (BindingIdentifier)

const arrowCreator = (payload: number) => ({ payload });
>                                            ^^^^^^^: number   (TyNumber) (ObjectProperty)

const arrowCreator = (payload: number) => ({ payload });
>                                            ^^^^^^^: number   (TyNumber) (IdentifierReference)

let arrowLabel = "arrow";
>   ^^^^^^^^^^: string   (TyString) (BindingIdentifier)

arrowCreator.type = arrowLabel;
>^^^^^^^^^^^^: { (payload: number): { payload: number; }; type: string; match(value: unknown): value is { payload: number; }; }   (TyObject) (IdentifierReference)

arrowCreator.type = arrowLabel;
>^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^: string   (TyString) (ExpressionStatement)

arrowCreator.type = arrowLabel;
>            ^^^^: string   (TyString) (StaticMemberExpression)

arrowCreator.type = arrowLabel;
>                   ^^^^^^^^^^: string   (TyString) (IdentifierReference)

arrowCreator.match = (value: unknown): value is { payload: number } => true;
>^^^^^^^^^^^^: { (payload: number): { payload: number; }; type: string; match(value: unknown): value is { payload: number; }; }   (TyObject) (IdentifierReference)

arrowCreator.match = (value: unknown): value is { payload: number } => true;
>^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^: (value: unknown) => value is { payload: number; }   (TyFunction) (ExpressionStatement)

arrowCreator.match = (value: unknown): value is { payload: number } => true;
>            ^^^^^: (value: unknown) => value is { payload: number; }   (TyFunction) (StaticMemberExpression)

arrowCreator.match = (value: unknown): value is { payload: number } => true;
>                     ^^^^^: unknown   (TyUnknown) (BindingIdentifier)

arrowCreator.match = (value: unknown): value is { payload: number } => true;
>                                      ^^^^^: unknown   (TyUnknown) (IdentifierName)

arrowCreator.match = (value: unknown): value is { payload: number } => true;
>                                                 ^^^^^^^: number   (TyNumber) (TSPropertySignature)

const arrowType = arrowCreator.type;
>     ^^^^^^^^^: string   (TyString) (BindingIdentifier)

const arrowType = arrowCreator.type;
>                 ^^^^^^^^^^^^: { (payload: number): { payload: number; }; type: string; match(value: unknown): value is { payload: number; }; }   (TyObject) (IdentifierReference)

const arrowType = arrowCreator.type;
>                              ^^^^: string   (TyString) (StaticMemberExpression)

const arrowMatch = arrowCreator.match;
>     ^^^^^^^^^^: (value: unknown) => value is { payload: number; }   (TyFunction) (BindingIdentifier)

const arrowMatch = arrowCreator.match;
>                  ^^^^^^^^^^^^: { (payload: number): { payload: number; }; type: string; match(value: unknown): value is { payload: number; }; }   (TyObject) (IdentifierReference)

const arrowMatch = arrowCreator.match;
>                               ^^^^^: (value: unknown) => value is { payload: number; }   (TyFunction) (StaticMemberExpression)

const arrowResult = arrowCreator(1);
>     ^^^^^^^^^^^: { payload: number; }   (TyObject) (BindingIdentifier)

const arrowResult = arrowCreator(1);
>                   ^^^^^^^^^^^^: { (payload: number): { payload: number; }; type: string; match(value: unknown): value is { payload: number; }; }   (TyObject) (IdentifierReference)
