declare const x: string | undefined;
>             ^: string | undefined   (TyUnion)

if (x) {
>   ^: string | undefined   (TyUnion)

  console.log(x);
> ^^^^^^^: Console   (TyTypeReference)

  console.log(x);
> ^^^^^^^^^^^^^^^: void   (TyVoid)

  console.log(x);
>         ^^^: (...data: any[]) => void   (TyFunction)

  console.log(x);
>             ^: string   (TyString)

  console.log(x);
> ^^^^^^^: Console   (TyTypeReference)

  console.log(x);
> ^^^^^^^^^^^^^^^: void   (TyVoid)

  console.log(x);
>         ^^^: (...data: any[]) => void   (TyFunction)

  console.log(x);
>             ^: string | undefined   (TyUnion)

declare const y: string | number | boolean;
>             ^: string | number | boolean   (TyUnion)

const z = typeof y === "string" ? y : undefined;
>     ^: string | undefined   (TyUnion)

const z = typeof y === "string" ? y : undefined;
>                ^: string | number | boolean   (TyUnion)

const z = typeof y === "string" ? y : undefined;
>                                 ^: string   (TyString)

const z = typeof y === "string" ? y : undefined;
>                                     ^^^^^^^^^: undefined   (TyUndefined)

if (typeof y === "string") {
>          ^: string | number | boolean   (TyUnion)

  console.log(y);
> ^^^^^^^: Console   (TyTypeReference)

  console.log(y);
> ^^^^^^^^^^^^^^^: void   (TyVoid)

  console.log(y);
>         ^^^: (...data: any[]) => void   (TyFunction)

  console.log(y);
>             ^: string   (TyString)

  console.log(y)
> ^^^^^^^: Console   (TyTypeReference)

  console.log(y)
> ^^^^^^^^^^^^^^: void   (TyVoid)

  console.log(y)
>         ^^^: (...data: any[]) => void   (TyFunction)

  console.log(y)
>             ^: number | boolean   (TyUnion)
